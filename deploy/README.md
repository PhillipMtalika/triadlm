---
title: TriadLM Compare
emoji: ⚖️
colorFrom: blue
colorTo: green
sdk: docker
app_port: 7860
pinned: false
---

# TriadLM — one prompt, four branches

Side-by-side demo of a 30M model across `base` / `sft` / `constitutional` /
`grounded` variants (see the TriadLM repo for training details + experiment log).
Checkpoints: `phillipmtalika/triadlm-m1-50m` (free Hub). No product names anywhere.
